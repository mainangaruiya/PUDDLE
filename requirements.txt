Django==4.2
gunicorn==20.1.0  # WSGI server for production (optional, for deployment)
python-dotenv==1.0.0  # For loading environment variables from .env files
Pillow==8.4.0  # For handling images (if you're working with media uploads)
django-environ==0.9.0  # For environment variable management (alternative to python-dotenv)

